export enum HandlerType {
  EVERY = 'every',
  SCHEDULE = 'schedule',
  NOW = 'now',
  READY = 'ready',
  ERROR = 'error',
  START = 'start',
  COMPLETE = 'complete',
  SUCCESS = 'success',
  FAIL = 'fail',
}
